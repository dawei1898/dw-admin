server:
  port: 8020

spring:
  application:
      name: dw-admin-app
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dwa?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false
    username:
    password:
  servlet:
    multipart:
      max-request-size: 10MB
      max-file-size: 5MB

# mybatis-plus
mybatis-plus:
  mapperLocations: classpath:mapper/*.xml


# log??
logging:
  config: classpath:logback/logback-spring.xml
  level:
    com.dw.admin.dao: debug

dwa:
  auth:
    cache-type: DB
    secret: wq8wqh87a6f-8hslu9wedhsad-89dsaud8j-d6dy-dsja
  redis:
    url: localhost:6379
    password:
  file:
    # 阿里云对象存储 OSS 配置
    oss:
      access-key: xxxxxx
      secret-key: xxxxxx
      endpoint: xxxxxx.aliyuncs.com
      bucket: xxxxxx-oss-bucket
      prefix-path: xxxxxx/
      url-expires: 604800